package zone.wim.token;

import zone.wim.exception.TypeException;

public class DataLocation {
	
	public static DataLocation parse(String className) throws Throwable {
		// TODO stub
		throw new TypeException.Invalid(className);
	}
}
