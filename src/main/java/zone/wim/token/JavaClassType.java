package zone.wim.token;

import zone.wim.exception.*;

public class JavaClassType implements Type {

	public static JavaClassType parse(String className) throws Throwable {
		// TODO stub
		throw new TypeException.Invalid(className);
	}
}
